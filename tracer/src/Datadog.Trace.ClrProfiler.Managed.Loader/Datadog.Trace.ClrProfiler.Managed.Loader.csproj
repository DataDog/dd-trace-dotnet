<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Datadog.Trace.ClrProfiler.Managed.Loader</RootNamespace>
    <TargetFrameworks>net461;netcoreapp2.0</TargetFrameworks>
    <!-- We exclude the configuration from the artifacts pivots here because it makes it easier -->
    <!-- to coordinate with the native build -->
    <ArtifactsPivots>$(TargetFramework.ToLowerInvariant())</ArtifactsPivots>
    <!-- NuGet -->
    <Version>3.9.0</Version>

    <!-- Hide warnings for EOL .NET Core targets (e.g. netcoreapp2.0) -->
    <CheckEolTargetFramework>false</CheckEolTargetFramework>
  </PropertyGroup>

</Project>