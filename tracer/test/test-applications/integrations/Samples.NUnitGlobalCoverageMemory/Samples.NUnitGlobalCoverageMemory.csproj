<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <OutputType>Library</OutputType>
    <IsPackable>false</IsPackable>
    <ApiVersion Condition="'$(ApiVersion)' == ''">3.2.0</ApiVersion>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <GenerateTargetFrameworkAttribute>false</GenerateTargetFrameworkAttribute>
    <GenerateRuntimeConfigurationFiles>true</GenerateRuntimeConfigurationFiles>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="NUnit" Version="3.13.3" />
    <PackageReference Include="NUnit3TestAdapter" Version="4.5.0" />
    <PackageReference Include="coverlet.collector" Version="$(ApiVersion)">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <Target Name="CodeCoverageFixTarget" AfterTargets="Build" Condition="'$(TargetFramework)' != '' AND '$(ManuallyCopyCodeCoverageFiles)' != 'false'">
    <Message Importance="High" Text="Fixing Code Coverage: $(TargetFramework)" />
    <CallTarget Targets="ComputeFilesToPublish" />
    <CallTarget Targets="CopyCoverletDataCollectorFiles" />

    <!-- Windows integration tests consume the standalone build output instead of the publish output.
         Keep Coverlet in a dedicated adapter directory so its PDBs are not instrumented by the
         global coverage stress test, which deliberately processes top-level sample assemblies. -->
    <ItemGroup>
      <PublishedTraceDataCollectorArtifacts Include="$(PublishDir)**\*.*" />
    </ItemGroup>
    <Move SourceFiles="@(PublishedTraceDataCollectorArtifacts)" DestinationFolder="$(OutputPath)\coverlet\%(RecursiveDir)" />
  </Target>

</Project>
