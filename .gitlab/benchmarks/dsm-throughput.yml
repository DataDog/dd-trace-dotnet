variables:
  DSM_THROUGHPUT_CI_IMAGE: 486234852809.dkr.ecr.us-east-1.amazonaws.com/ci/benchmarking-platform:dd-trace-dotnet-dsm
  MACROBENCHMARKS_CI_IMAGE: 486234852809.dkr.ecr.us-east-1.amazonaws.com/ci/benchmarking-platform:dotnet-throughput-7

stages:
  - check-azure-pipeline
  - benchmarks

workflow:
  auto_cancel:
    on_new_commit: interruptible

check_azure_pipeline:
  stage: check-azure-pipeline
  image: $MACROBENCHMARKS_CI_IMAGE
  script:
    - git clone --branch dd-trace-dotnet/macro https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.ddbuild.io/${GITLAB_GROUP}/benchmarking-platform platform && cd platform
    - ./wait-for-pipeline.sh
  artifacts:
    name: "artifacts"
    when: always
    paths:
      - artifacts/
      - build-id.txt
    expire_in: 3 months
  tags: ["arch:amd64"]
  timeout: 1h
  rules:
    - if: $CI_COMMIT_REF_NAME == "master"
      interruptible: false
    - interruptible: true

dsm_throughput:
  stage: benchmarks
  tags: ["runner:apm-k8s-same-cpu"]
  needs: ["check_azure_pipeline"]
  image: $DSM_THROUGHPUT_CI_IMAGE
  timeout: 1h
  script:
    - source build-id.txt
    - export ARTIFACTS_DIR="$(pwd)/artifacts" && (mkdir "${ARTIFACTS_DIR}" || :)
    # Redirect GitHub clone URLs to GitLab mirrors. Both orgs are covered:
    # - DataDog: OSS org (stays)
    # - GITHUB_ORG_INTERNAL: GitHub org for internal repos
    - git config --global url."https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.ddbuild.io/${GITLAB_GROUP}/".insteadOf "https://github.com/DataDog/"
    - git config --global url."https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.ddbuild.io/${GITLAB_GROUP}/".insteadOf "https://github.com/${GITHUB_ORG_INTERNAL}/"
    - git clone --branch augusto/dd-trace-dotnet-dsm-flakiness https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.ddbuild.io/${GITLAB_GROUP}/benchmarking-platform platform && cd platform
    - ./steps/setup-sut.sh
    - bp-runner bp-runner.yml --debug
  artifacts:
    name: "artifacts"
    when: always
    paths:
      - artifacts/
    expire_in: 3 months
  rules:
    - if: $CI_COMMIT_REF_NAME == "master"
      interruptible: false
    - interruptible: true
