imports:
    - /var/opt/crank/variables.yml

profiles:

  windows:
    variables:
      serverAddress: "{{ windowsIp }}"
    jobs:
      application:
        endpoints:
          - "{{ windowsEndpoint }}"
        environmentVariables:
          COR_PROFILER: "{846F5F1C-F9AE-4B07-969E-05C26BC060D8}"
          COR_PROFILER_PATH: "{{ windowsProfilerPath }}\\{{ commit_hash }}\\monitoring-home-windows\\Datadog.AutoInstrumentation.NativeLoader.x64.dll"
          CORECLR_PROFILER: "{846F5F1C-F9AE-4B07-969E-05C26BC060D8}"
          CORECLR_PROFILER_PATH: "{{ windowsProfilerPath }}\\{{ commit_hash }}\\monitoring-home-windows\\Datadog.AutoInstrumentation.NativeLoader.x64.dll"
          DD_DOTNET_TRACER_HOME: "{{ windowsProfilerPath }}\\{{ commit_hash }}\\monitoring-home-windows\\tracer"
          DD_DOTNET_PROFILER_HOME: "{{ windowsProfilerPath }}\\{{ commit_hash }}\\monitoring-home-windows\\continousprofiler"
          DD_AGENT_HOST: "{{ controllerIp }}"
          DD_TRACE_LOGGING_RATE: 6
          DD_TRACE_DEBUG: 1
          DD_TRACE_ENABLED: 0
          DD_ENV: throughput-profiler-windows
        options:
          requiredOperatingSystem: windows
          requiredArchitecture: x64
          buildFiles:
          - "../../../monitoring-home-windows/**;{{ windowsProfilerPath }}\\{{ commit_hash }}\\monitoring-home-windows"
      load:
        endpoints:
          - http://localhost:5010

  linux:
    variables:
      serverAddress: "{{ linuxIp }}"
    jobs:
      application:
        endpoints:
          - "{{ linuxEndpoint }}"
        environmentVariables:
          COR_PROFILER: "{BD1A650D-AC5D-4896-B64F-D6FA25D6B26A}"
          COR_PROFILER_PATH: "{{ linuxProfilerPath }}/{{ commit_hash }}/profiler-home-linux/Datadog.AutoInstrumentation.Profiler.Native.x64.so"
          CORECLR_PROFILER: "{BD1A650D-AC5D-4896-B64F-D6FA25D6B26A}"
          CORECLR_PROFILER_PATH: "{{ linuxProfilerPath }}/{{ commit_hash }}/profiler-home-linux/Datadog.AutoInstrumentation.Profiler.Native.x64.so"
          DD_DOTNET_TRACER_HOME: "{{ linuxProfilerPath }}/{{ commit_hash }}/tracer-home-linux"
          DD_DOTNET_PROFILER_HOME: "{{ linuxProfilerPath }}/{{ commit_hash }}/profiler-home-linux"
          DD_AGENT_HOST: "{{ controllerIp }}"
          DD_TRACE_LOGGING_RATE: 6
          DD_TRACE_DEBUG: 1
          DD_TRACE_ENABLED: 0
          DD_ENV: throughput-profiler-linux
          LD_LIBRARY_PATH: "{{ linuxProfilerPath }}/{{ commit_hash }}/profiler-home-linux"
          LD_PRELOAD: "{{ linuxProfilerPath }}/{{ commit_hash }}/profiler-home-linux/Datadog.Linux.ApiWrapper.x64.so"
        options:
          requiredOperatingSystem: linux
          requiredArchitecture: x64
          buildFiles:
          - "../../../profiler-home-linux/**;{{ linuxProfilerPath }}/{{ commit_hash }}/profiler-home-linux"
      load:
        endpoints:
          - http://localhost:5010
