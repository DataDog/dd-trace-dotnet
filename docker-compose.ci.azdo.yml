version: '3'
services:
  ExplorationTests:
    networks:
      - azdo_network

networks:
  azdo_network:
    external:
      ## Insert the name of the network dynamically or else the configuration will be invalid